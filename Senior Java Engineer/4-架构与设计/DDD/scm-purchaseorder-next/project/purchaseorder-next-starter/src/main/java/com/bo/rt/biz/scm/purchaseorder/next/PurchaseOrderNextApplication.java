package com.bo.rt.biz.scm.purchaseorder.next;

/**
 * 新采购订单服务启动入口。
 *
 * <p>当前不绑定具体 Spring Boot 注解，后续按公司脚手架规范补齐启动配置。</p>
 */
public class PurchaseOrderNextApplication {

    /**
     * 服务启动方法。
     *
     * @param args 启动参数
     */
    public static void main(String[] args) {
        System.out.println(
                "scm-purchaseorder-next pseudocode is ready; "
                        + "wire the application services with the company Spring Boot starter."
        );
    }
}
