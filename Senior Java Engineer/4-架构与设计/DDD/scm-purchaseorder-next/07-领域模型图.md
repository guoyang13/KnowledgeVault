# 领域模型图

> [!warning] 第一版模型图
> 本文用于保留第一版设计演进记录。结合原服务代码审计后，限界上下文和聚合边界已调整，请以 [[10-限界上下文与上下文映射-第二版]] 与 [[11-采购订单聚合与状态模型-第二版]] 为准。

## 1. 模型阅读说明

这份图不是数据库 ER 图，而是新 `scm-purchaseorder-next-service` 的领域模型图。它表达的是：

- 哪些业务概念属于同一个限界上下文。
- 哪些对象是聚合根，哪些对象只能通过聚合根修改。
- 哪些规则应该放在领域模型里，而不是散落在应用服务或 Repository 里。
- 哪些外部系统需要通过防腐层接入。

## 2. 限界上下文图

```mermaid
flowchart LR
  subgraph NEXT["scm-purchaseorder-next-service"]
    REQ["采购需求上下文<br/>Requisition<br/>PR / PRD / 供应商确认 / 异常确认"]
    PO["采购订单上下文<br/>Purchase Order<br/>PO / PSO / 提交 / 审批 / 发货 / 收货 / 入库"]
    EXP["出口履约上下文<br/>Export Shipment<br/>待出口 / 货柜 / 装柜 / 车辆检查"]
    INS["资质商检上下文<br/>Inspection<br/>资质校验 / 商检资料 / 商检回调"]
    SETTLE["结算费用上下文<br/>Settlement<br/>费用初始化 / 结算推送 / 畅收付状态"]
    WARN["异常预警上下文<br/>Exception & Warning<br/>交期异常 / 超期批次 / 跟单提醒"]
    FILE["导入导出上下文<br/>File Operation<br/>Excel / PDF / 异步文件任务"]
  end

  REQ -->|"转 PO"| PO
  PO -->|"待出口单据"| EXP
  PO -->|"资质与商检要求"| INS
  PO -->|"费用和结算事件"| SETTLE
  PO -->|"状态变化 / 异常事实"| WARN
  FILE -->|"批量导入命令"| REQ
  FILE -->|"批量导入命令"| PO
  FILE -->|"导出读模型"| EXP
```

设计约束：

- `PurchaseOrder` 是第一阶段核心上下文。
- `PurchaseRequisition` 与 `PurchaseOrder` 通过业务事件和编码关联，不在一个大聚合里。
- 货柜、商检、结算、异常预警都可以订阅采购订单事件，但不能绕过采购订单聚合直接修改 PO/PSO 状态。

## 3. 核心聚合模型图

```mermaid
classDiagram
direction LR

class PurchaseRequisition {
  <<AggregateRoot>>
  +PurchaseRequisitionCode code
  +RequisitionStatus status
  +supplierConfirm()
  +confirmException()
  +cancel()
  +markTransferredToPo()
}

class PurchaseRequisitionDetail {
  <<Entity>>
  +SkuId skuId
  +Quantity requiredQuantity
  +Quantity satisfiableQuantity
  +ExceptionReason exceptionReason
}

class PurchaseOrder {
  <<AggregateRoot>>
  +PurchaseOrderCode code
  +PurchaseOrderType type
  +PurchaseOrderStatus status
  +SupplierRef supplier
  +Destination destination
  +DeliveryTerm deliveryTerm
  +createDraft()
  +replaceDraftLines()
  +submit()
  +approve()
  +reject()
  +cancel()
  +confirmSupplierDelivery()
  +markTransitReceived()
  +markTransitInbounded()
  +markDestinationReceived()
  +markDestinationInbounded()
}

class PurchaseSubOrder {
  <<Entity>>
  +PurchaseSubOrderCode code
  +SkuId skuId
  +GoodsType goodsType
  +Quantity plannedQuantity
  +Money unitPriceIncludingTax
  +Money totalPriceIncludingTax
  +TaxRate taxRate
  +PurchaseSubOrderStatus status
  +markStatus()
  +recalculateAmount()
}

class PurchaseCabinet {
  <<AggregateRoot>>
  +CabinetCode cabinetCode
  +CabinetStatus status
  +create()
  +updateInfo()
  +markLoaded()
  +submitVehicleCheck()
}

class CabinetDetail {
  <<Entity>>
  +PurchaseOrderCode poCode
  +PurchaseSubOrderCode psoCode
  +SkuId skuId
  +Quantity loadedQuantity
}

class InspectionOrder {
  <<AggregateRoot>>
  +InspectionCode code
  +PurchaseOrderCode poCode
  +InspectionStatus status
  +submitMaterial()
  +pass()
  +reject()
}

class SettlementOrder {
  <<AggregateRoot>>
  +SettlementCode code
  +PurchaseOrderCode poCode
  +SettlementStatus status
  +initializeFee()
  +pushToLinkup()
  +markPushed()
}

class Money {
  <<ValueObject>>
  +amount
  +currency
  +add()
  +multiply()
}

class Quantity {
  <<ValueObject>>
  +value
  +add()
  +subtract()
}

class Destination {
  <<ValueObject>>
  +type
  +code
  +name
}

class SupplierRef {
  <<ValueObject>>
  +supplierCode
  +supplierName
}

PurchaseRequisition "1" *-- "1..n" PurchaseRequisitionDetail
PurchaseOrder "1" *-- "1..n" PurchaseSubOrder
PurchaseOrder --> SupplierRef
PurchaseOrder --> Destination
PurchaseSubOrder --> Money
PurchaseSubOrder --> Quantity
PurchaseCabinet "1" *-- "1..n" CabinetDetail
PurchaseCabinet --> PurchaseOrder : references by code
InspectionOrder --> PurchaseOrder : references by code
SettlementOrder --> PurchaseOrder : references by code
```

聚合边界原则：

- `PurchaseOrder` 聚合内强一致维护 PO/PSO 状态、数量、金额、目的地和采购类型约束。
- `PurchaseCabinet`、`InspectionOrder`、`SettlementOrder` 只通过 `PurchaseOrderCode` 引用 PO，不持有完整 `PurchaseOrder` 对象。
- `Money`、`Quantity`、`Destination`、`SupplierRef` 作为值对象，避免基础字段散落。
- 页面列表、导出报表、看板统计不进入聚合，放到 Query Model。

## 4. 领域事件模型图

```mermaid
flowchart TB
  PO["PurchaseOrder 聚合"] --> E1["PurchaseOrderCreated"]
  PO --> E2["PurchaseOrderSubmitted"]
  PO --> E3["PurchaseOrderApproved"]
  PO --> E4["PurchaseOrderCancelled"]
  PO --> E5["SupplierDeliveryConfirmed"]
  PO --> E6["TransitWarehouseInbounded"]
  PO --> E7["DestinationInbounded"]

  REQ["PurchaseRequisition 聚合"] --> R1["PurchaseRequisitionSupplierConfirmed"]
  REQ --> R2["PurchaseRequisitionTransferredToPo"]
  REQ --> R3["PurchaseRequisitionCancelled"]

  E2 --> APPROVAL["审批上下文<br/>ApprovalWorkflowPort"]
  E3 --> WMS["仓储/WMS<br/>WarehouseFulfillmentPort"]
  E3 --> INS["资质商检<br/>Inspection 上下文"]
  E5 --> EXP["出口履约<br/>ExportShipment 上下文"]
  E6 --> STOCK["库存中台<br/>StockIntegrationPort"]
  E7 --> SETTLE["结算费用<br/>Settlement 上下文"]
  E4 --> WARN["异常预警<br/>ExceptionWarning 上下文"]
  R2 --> PO
```

事件设计规则：

- 领域事件只表达“已经发生的业务事实”，例如 `PurchaseOrderApproved`。
- 跨系统通知使用集成事件，从领域事件转换而来。
- 生产副作用必须通过 outbox 或等价可靠消息机制 afterCommit 发布。
- 每个集成事件必须有幂等键，优先使用业务单号 + 事件类型 + 版本号。

## 5. 防腐层与端口图

```mermaid
flowchart LR
  APP["Application Service"] --> DOMAIN["Domain Model"]
  APP --> PORTS["Ports"]

  subgraph PORTS["端口接口"]
    GOODS_PORT["GoodsCatalogPort"]
    SUPPLIER_PORT["SupplierProfilePort"]
    WH_PORT["WarehouseDirectoryPort"]
    APPROVAL_PORT["ApprovalWorkflowPort"]
    STOCK_PORT["StockIntegrationPort"]
    WMS_PORT["WmsIntegrationPort"]
    SETTLE_PORT["SettlementPort"]
    ORDERNO_PORT["OrderNoPort"]
    EVENT_PORT["DomainEventPublisher"]
  end

  subgraph ACL["Infrastructure ACL / Adapter"]
    GOODS_ACL["商品防腐层"]
    SUPPLIER_ACL["供应商防腐层"]
    WH_ACL["仓库防腐层"]
    APPROVAL_ACL["审批防腐层"]
    STOCK_ACL["库存防腐层"]
    WMS_ACL["WMS防腐层"]
    SETTLE_ACL["结算防腐层"]
    ORDERNO_ACL["单号防腐层"]
    EVENT_ACL["事件发布适配器"]
  end

  GOODS_PORT -.impl.-> GOODS_ACL --> GOODS["rt-mid-goods-service"]
  SUPPLIER_PORT -.impl.-> SUPPLIER_ACL --> SUPPLIER["scm-supplier-service"]
  WH_PORT -.impl.-> WH_ACL --> WH["warehouse/basic-config"]
  APPROVAL_PORT -.impl.-> APPROVAL_ACL --> APPROVAL["rt-basic-approval-service"]
  STOCK_PORT -.impl.-> STOCK_ACL --> STOCK["rt-mid-stock-service"]
  WMS_PORT -.impl.-> WMS_ACL --> WMS["WMS / scm-gateway"]
  SETTLE_PORT -.impl.-> SETTLE_ACL --> SETTLE["Linkup / AP"]
  ORDERNO_PORT -.impl.-> ORDERNO_ACL --> BASE["base-config order no"]
  EVENT_PORT -.impl.-> EVENT_ACL --> MQ["RocketMQ / Outbox"]
```

防腐层规则：

- 外部 DTO 只能存在于 `infrastructure.acl`。
- 应用层拿到的是本地语言对象，例如 `SupplierSnapshot`、`GoodsSnapshot`、`WarehouseSnapshot`。
- 领域层只依赖本地值对象和端口接口，不感知 Feign、MQ、Mapper。

## 6. 第一阶段建模重点

第一阶段只做采购订单主生命周期的领域闭环：

```text
创建草稿
→ 提交
→ 审批通过/驳回
→ 供应商确认发货
→ 中转仓/目的地收货
→ 入库完成
→ 作废/完成
```

暂不把 Excel、PDF、报表、历史数据修复放进领域模型。它们应该作为应用层或查询层能力存在，避免污染核心采购模型。
