---
aliases:
  - 神经网络与反向传播
---

# 神经网络与反向传播

> 理解 LLM 的**最小数学栈**：可微分的函数堆叠 + 用梯度下降从数据里学参数。Transformer 再复杂，训练仍依赖这一套。

## 从神经元到网络

- **神经元**：\(y = \sigma(w^\top x + b)\)，\(\sigma\) 为非线性**激活函数**（ReLU、GELU、SiLU 等）
- **层**：把许多神经元并排/堆叠；**深度网络** = 多层非线性变换
- **前向传播**：输入 → 逐层计算 → 得到预测（对 LLM 即下一个 token 的 logits）

## 损失函数（Loss）

衡量「预测」与「目标」的差距；训练就是**最小化损失**。

| 场景 | 典型损失 |
|------|----------|
| LLM 预训练 / SFT | **交叉熵（Cross-Entropy）**：对每个位置的真实下一 token，惩罚模型给出的错误概率 |
| 回归 | MSE |
| 分类 | 交叉熵（多类） |

语言模型里：位置 \(t\) 的真实 token 为 \(y_t\)，模型输出分布 \(\hat{P}(\cdot \mid x_{<t})\)，损失约为 \(-\log \hat{P}(y_t \mid x_{<t})\)，全序列求平均。

## 反向传播（Backpropagation）

- **链式法则**：从损失 \(L\) 往回算每一层参数对 \(L\) 的梯度 \(\partial L / \partial w\)
- **计算图**：框架（PyTorch、JAX）自动构建图并求导
- **批量训练**：每次用一小批（mini-batch）样本估计梯度，更新参数

## 一次训练 step 长什么样

```text
1. 取一批文本，切成 token 序列
2. 前向传播：模型预测每个位置的下一个 token
3. 计算 loss：预测分布和真实 token 的差距
4. 反向传播：计算每个参数对 loss 的影响方向
5. 优化器更新参数：让下次预测更接近真实数据
```

这个循环重复数十亿到数万亿 token 后，模型参数里就压缩进了大量语言模式、世界关联、代码结构和常见推理模板。

## 优化器（Optimizer）

在梯度方向上更新权重，常用：

- **SGD + Momentum**
- **Adam / AdamW** —— LLM 预训练与微调的事实标准之一
- **学习率调度**：warmup、cosine decay 等，影响收敛与稳定性

## 与 LLM 的对应关系

| 基础概念 | 在 LLM 中 |
|----------|----------|
| 参数 \(w\) | 数十亿～千亿权重（Embedding、Attention、FFN） |
| 前向 | 一次 forward 得到整段 logits |
| 损失 | 因果语言建模的 token 级交叉熵（通常只算「预测下一 token」的位置） |
| 反向 | 全参数梯度；规模极大 → 需分布式训练、混合精度、梯度检查点 |

## 你需要抓住的直觉

- **梯度不是“答案”，而是“怎么调参数会让错误变小”。**
- **训练是并行的**：一段文本里每个位置都可以贡献一个 next-token 预测任务。
- **推理是串行的**：生成时必须先拿到当前 token，才能继续预测下一个。
- **预训练和微调本质相近**：都是前向、算 loss、反向更新，只是数据、目标和更新范围不同。
- **LoRA 等高效微调**不是重写训练原理，而是只训练少量附加参数，见 [[AI/00-AI知识体系/概念/02-训练与推理/05-高效微调|高效微调]]。

## 不必深挖（应用工程师可跳过）

- 手工推导每一层 Jacobian
- 二阶优化、贝叶斯神经网络
- 从零实现 CUDA kernel（交给框架与 [[AI/00-AI知识体系/概念/02-训练与推理/06-推理加速三件套|推理加速]] 栈）

## 学习检查

1. 为什么 LLM 训练常用交叉熵？
2. 为什么训练可以并行看整段文本，但推理必须逐 token 生成？
3. AdamW、学习率、batch size 改变的是训练过程的哪一部分？

## 与之相关

- 前置：[[AI/00-AI知识体系/概念/00-基础与LLM概论/01-什么是LLM|什么是LLM]]
- 表示：[[AI/00-AI知识体系/概念/00-基础与LLM概论/03-表示学习-从词嵌入到上下文|表示学习-从词嵌入到上下文]]
- 目标：[[AI/00-AI知识体系/概念/00-基础与LLM概论/04-语言模型与Next-Token-Prediction|语言模型与 Next-Token Prediction]]
- 架构：[[AI/00-AI知识体系/概念/01-模型层/01-Transformer架构|Transformer架构]]
- 微调：[[AI/00-AI知识体系/概念/02-训练与推理/05-高效微调|高效微调]]

## 延伸阅读

- 3Blue1Brown — 神经网络系列（直觉）
- Andrej Karpathy — *Neural Networks: Zero to Hero*（micrograd）
- Goodfellow et al., *Deep Learning* — 第 6–8 章（优化与反向传播）
